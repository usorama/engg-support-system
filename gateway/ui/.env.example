# ESS Chat UI Configuration
# Copy this to .env.local for development

# API Configuration
# In development, the Vite proxy handles /api -> localhost:3001
# In production, set this to your Gateway URL
VITE_API_URL=/api

# API Key for authenticated endpoints (optional in development)
# Get this from your ESS_API_KEY environment variable
VITE_ESS_API_KEY=

# Build Configuration
# Set to 'production' for optimized builds
NODE_ENV=development
