# Veracity Engine Environment Configuration
# Copy this file to .env and customize for your deployment
# DO NOT commit .env files with real credentials to version control.
#
# Configuration Hierarchy (highest to lowest priority):
# 1. CLI arguments
# 2. VERACITY_* environment variables (new format)
# 3. Legacy environment variables (NEO4J_*, EMBED_MODEL, etc.)
# 4. Config file (config/veracity.yaml)
# 5. Default values

# ==============================================================================
# Neo4j Configuration (Legacy format - still supported)
# ==============================================================================
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=your_secure_password_here

# Neo4j Docker auth string (format: username/password)
NEO4J_AUTH=neo4j/your_secure_password_here

# Neo4j Paths
NEO4J_DATA_PATH=/var/lib/neo4j/data
NEO4J_LOGS_PATH=/var/log/neo4j

# ==============================================================================
# Embedding Model Configuration
# ==============================================================================
EMBED_MODEL=nomic-embed-text
# EMBED_MODEL_VERSION=v1.5.0  # For STORY-002: version pinning

# ==============================================================================
# LLM Configuration
# ==============================================================================
LLM_MODEL=llama3.2
LLM_SEED=42
# LLM_MODEL_VERSION=3.2.3  # For STORY-002: version pinning

# ==============================================================================
# Veracity Engine Configuration (New VERACITY_* prefix format)
# ==============================================================================
# These use the new hierarchical config system with __ as nested delimiter
# Example: VERACITY_NEO4J__URI=bolt://localhost:7687

# Logging
VERACITY_LOGGING__LEVEL=INFO

# Veracity thresholds (for STORY-012)
VERACITY_STALENESS_THRESHOLD_DAYS=90
VERACITY_ORPHAN_THRESHOLD_NEIGHBORS=2

# ==============================================================================
# UI Configuration (Vite environment variables)
# ==============================================================================
VITE_NEO4J_URI=bolt://localhost:7687
VITE_NEO4J_USER=neo4j
VITE_NEO4J_PASSWORD=your_secure_password_here

# ==============================================================================
# Deployment Configuration (for STORY-005)
# ==============================================================================
DEPLOYMENT_MODE=vps
HEALTH_CHECK_INTERVAL=30s
HEALTH_CHECK_TIMEOUT=10s
HEALTH_CHECK_RETRIES=3
